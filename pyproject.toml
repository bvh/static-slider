[project]
name = "static-slider"
version = "0.1.0"
description = "HTML/CSS gallery and slider widget, built with minimal JavaScript."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
]
